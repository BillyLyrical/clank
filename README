
Clank - A Perl Hybrid AI Harness
================================

Clank is an over-engineered Perl shell for the age of AI.

LLMs are fat-fingered geniuses.
Logic engines are idiot savants.
Clank bridges them in Perl.

Clank is a neurosymbolic AI harness that combines LLM pattern
recognition with explicit world models, symbolic reasoning, and
deterministic rules. Entities, relations, temporal facts, causal
links, and beliefs live in a shared SQLite blackboard. Datalog,
FSMs, behavior trees, and SAT solvers reason over that world
model. LLMs generate and critique. The bus coordinates everything.

Perl is the backbone because Larry Wall designed it to fit human
language instincts, and LLMs are trained on that text. The harness
can read, write, and extend its own infrastructure. That's the
thesis, and the working codebase proves it.

Perl has excellent pattern matching, text manipulation, speed,
integration with unix, tainting, and a massive CPAN library,
that is battle tested, carefully curated and trivial to install.

I find that Perls sigils make it easier for LLMs to understand
the basic syntax of small chunks of code, saving context tokens.
Perls rich density of meaning, makes the LLM be able to express itself
more concisely, which preserves context.

What's in the box
------------------

Core harness:
  - Agent loop (ported from Pi)
  - 4 tools: read, bash, edit, write
  - SQLite store + pub/sub bus
  - LLM providers: Ollama, OpenAI, Anthropic, Gemini, Azure, LMStudio
  - Context engineering: capability manifest, RATS tool selection,
    knowledge bus, context compression, behavioral rules

125 curated wits across 10 decks:

  logic     51 wits   Datalog engine, rules DSL, FSM, behavior trees, SAT
  critic    12 wits   Code critique heuristics
  git       10 wits   Git operations (status, log, diff, blame, ...)
  fs        12 wits   Filesystem operations (read, write, edit, ...)
  db         8 wits   Database shell (connect, query, execute, ...)
  perl      16 wits   Perl development (syntax, review, POD, tests)
  psh        3 wits   Perl Shell REPL (eval, vars, help)
  search     9 wits   Local + web search
  embedding  1 wit    Semantic search via embeddings
  neuro      3 wits   Neurosymbolic integration (world model, crystallizer, constraints)

  768 tests, all offline. prove -l t/ green.


Quick start
-----------

  # with LM Studio running locally:
  clank

  # with Ollama:
  CLANK_PROVIDER=ollama CLANK_MODEL=codellama clank

  # with OpenAI:
  CLANK_PROVIDER=openai CLANK_MODEL=gpt-4o clank

  # with Anthropic:
  CLANK_PROVIDER=anthropic CLANK_MODEL=claude-sonnet-4-20250514 clank

  # daemon mode (for programmatic access from any language):
  clankd --stdio --provider lmstudio --model qwen3.8-27b

  # programmatic (in-process):
  perl -Ilib -e '
    use Clank::Driver;
    my $d = Clank::Driver->new(provider=>"ollama", model=>"codellama");
    $d->start;
    my $r = $d->ask("list the files in the current directory");
    print $r->{response};
    $d->close;
  '


Development setup
-----------------

  cd clank
  perl link_wits.pl        # symlink wits/ into lib/
  prove -l t/              # run core tests
  prove -l wits/*/t/       # run wit tests

link_wits.pl creates relative symlinks from lib/Clank/Wits/* to
each wit's lib/ directory. This makes the dev tree behave like an
installed CPAN tree — all modules load from -Ilib.

  perl link_wits.pl --clean   # remove symlinks

The symlinks are gitignored. Run link_wits.pl after clone.


The Perl thesis
---------------

Perl is the foundation of a new kind of execution environment.

In a standard AI harness, the LLM generates code and the harness runs it.
In Clank, the LLM generates Perl, and Perl IS the environment.
The harness can modify itself, it can extend the harness by writing wits.
Logic engines can collapse expensive LLM calls into fast deterministic rules.
The system gets cheaper and faster the more it's used — crystallization
turns LLM solutions into deterministic Perl rules that run forever.

Context engineering means Clank scales to hundreds of wits without burning
50k tokens per call. A capability manifest, retrieval-augmented tool
selection, a knowledge bus, and behavioral rules compose ~5-8k tokens of
targeted context instead of dumping everything into the prompt.


Questions for testers
---------------------

Clank is new and I'm still figuring things out.
I need your help thinking through these:

The UI — What does a great AI harness UI look like?
I have a simple REPL (Term::ReadLine) and an Unix Socket daemon. 
I'm working on a TUI, & A web interface etc.
What would make you actually use this daily?

Tools — We ship read, bash, edit, write (Pi parity) plus 88 wits.
What tools are missing? What would make Clank indispensable for your
workflow? Database migrations? Docker management? CPAN module
authoring? CPANTEST reporting?

The blackboard — Minsky's Society of Mind says intelligence
emerges from many simple agents communicating via a shared memory.
Our bus is the blackboard. The logic engines, the LLM, and the
user all publish and subscribe. Does this architecture hold up in
practice? Or is it over-engineered for what people actually need?

Crystallization — When the LLM solves a problem, can we capture
the solution as a deterministic Perl rule that runs faster and
cheaper forever? The logic deck makes this possible. Is it
actually useful? What would you crystallize?

The Perl hook — LLMs can read and write Perl natively. This means
Clank can modify its own wits, rewrite its own tools, and evolve
during a session. Is this a feature or a footgun? How do we make
it safe?

What can Clank unlock — I think a Perl AI environment enables
things no other harness can: self-modifying code, deterministic
reasoning over probabilistic generation, a complete unix shell
experience with AI assistance. What possibilities do you see that
I haven't thought of?


Get involved
------------

Clank is open source. The repo is at git@psycho:~/clank.git.

  git clone git@psycho:~/clank.git
  cd clank
  perl link_wits.pl
  prove -l t/

Read docs/ROADMAP.md for the full architecture and vision.
Read docs/Wits.md for the wit system design.
Read docs/DRIVER.md for the programmatic API.
Read docs/CONTEXT.md for the context engineering system.

If you're a Perl developer who wants to build the future of
AI-assisted development, we want to hear from you.
